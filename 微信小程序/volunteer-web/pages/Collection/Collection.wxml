<view>
	<van-toast id="van-toast" />
	<van-dialog id="van-dialog" />
	<YLM-Table bind:onClick="onClick" config="{{tableConfig}}" rows="{{rows}}" keys="{{keys}}" VanIcon="{{VanIcon}}"
		starO="{{starO}}" headers="{{headers}}"></YLM-Table>
</view>